services:
  - type: web
    name: matthews-terminal-app
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_BRIDGE_URL
        value: wss://matthews-terminal.onrender.com
